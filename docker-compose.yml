services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: predict-lotto-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-predict_lotto_nz}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password}
    ports:
      - "${POSTGRES_PORT:-5434}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./scripts/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
    networks:
      - predict-lotto-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-predict_lotto_nz}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: predict-lotto-redis
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    networks:
      - predict-lotto-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    command: redis-server --appendonly yes

  # .NET Core Backend API
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: predict-lotto-backend
    environment:
      ASPNETCORE_ENVIRONMENT: ${ASPNETCORE_ENVIRONMENT:-Development}
      ASPNETCORE_URLS: http://+:80
      ConnectionStrings__DefaultConnection: "Host=postgres;Port=5432;Database=${POSTGRES_DB:-predict_lotto_nz};Username=${POSTGRES_USER:-postgres};Password=${POSTGRES_PASSWORD:-password}"
      REDIS_CONNECTION_STRING: "redis:6379"
      PredictorService__BaseUrl: ${PREDICTOR_SERVICE_URL:-http://predictor:8000}
      AWS__Region: ${AWS_REGION:-us-west-2}
      AWS__AccessKeyId: ${AWS_ACCESS_KEY_ID}
      AWS__SecretAccessKey: ${AWS_SECRET_ACCESS_KEY}
      OpenAI__ApiKey: ${OPENAI_API_KEY}
    ports:
      - "${BACKEND_PORT:-5000}:80"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      predictor:
        condition: service_started
    networks:
      - predict-lotto-network
    volumes:
      - ./backend/uploads:/app/uploads
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    restart: unless-stopped

  # Vue.js Frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VUE_APP_API_BASE_URL: ${VUE_APP_API_BASE_URL:-http://localhost:5000}
    container_name: predict-lotto-frontend
    environment:
      VUE_APP_API_BASE_URL: ${VUE_APP_API_BASE_URL:-http://localhost:5000}
    ports:
      - "${FRONTEND_PORT:-3000}:80"
    depends_on:
      - backend
    networks:
      - predict-lotto-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:80/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    restart: unless-stopped

  # Python FastAPI Predictor Service
  predictor:
    build:
      context: ./predictor
      dockerfile: Dockerfile
    container_name: predict-lotto-predictor
    environment:
      PYTHONPATH: /app
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      MODEL_PATH: ${MODEL_PATH:-/app/models}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      # GroqCloud Configuration
      GROQCLOUD_API_KEY: ${GROQCLOUD_API_KEY}
      GROQCLOUD_MODEL: ${GROQCLOUD_MODEL:-llama-3.3-70b-versatile}
      GROQCLOUD_TEMPERATURE: ${GROQCLOUD_TEMPERATURE:-0.7}
      GROQCLOUD_MAX_TOKENS: ${GROQCLOUD_MAX_TOKENS:-4096}
      GROQCLOUD_BASE_URL: ${GROQCLOUD_BASE_URL:-https://api.groq.com/openai/v1}
      # LangSmith Configuration
      LANGCHAIN_TRACING_V2: ${LANGCHAIN_TRACING_V2:-true}
      LANGCHAIN_ENDPOINT: ${LANGCHAIN_ENDPOINT:-https://api.smith.langchain.com}
      LANGCHAIN_API_KEY: ${LANGCHAIN_API_KEY}
      LANGCHAIN_PROJECT: ${LANGCHAIN_PROJECT:-predict-lotto-nz}
    ports:
      - "${PREDICTOR_PORT:-8000}:8000"
    networks:
      - predict-lotto-network
    volumes:
      - predictor_models:/app/models
      - predictor_data:/app/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    restart: unless-stopped

networks:
  predict-lotto-network:
    driver: bridge
    name: predict-lotto-network

volumes:
  postgres_data:
    driver: local
    name: predict-lotto-postgres-data
  redis_data:
    driver: local
    name: predict-lotto-redis-data
  predictor_models:
    driver: local
    name: predict-lotto-predictor-models
  predictor_data:
    driver: local
    name: predict-lotto-predictor-data